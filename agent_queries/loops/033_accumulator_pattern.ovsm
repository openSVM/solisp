;; Query: Create Accumulate values in loop
;; Category: Loops
;; Expected: 120 (integer)
;; Demonstrates: variable definition, mutation, for loops

(define result 1)  ;; Create variable 'result'
(for (i (range 1 6))  ;; Iterate over collection
(set! result (* result i)))  ;; Update result
result
