;; Query: Calculate Sum all matrix elements
;; Category: Advanced
;; Expected: 45 (integer)
;; Demonstrates: variable definition, mutation, for loops, arrays

(define matrix [[1 2 3] [4 5 6] [7 8 9]])  ;; Create variable 'matrix'
(define sum 0)  ;; Create variable 'sum'
(for (row matrix)  ;; Iterate over collection
(for (val row)  ;; Iterate over collection
(set! sum (+ sum val))))  ;; Update sum
sum
